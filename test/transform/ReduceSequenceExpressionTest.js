var Helper = require('../Helper');

describe('Reduce sequence expression', function() {
    it('should remove clean values', function() {
        Helper.assertMatch('ReduceSequenceExpression');
    });
});
